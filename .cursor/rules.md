This project is a Flutter application.

Rules for AI modifications:

1. Do not rename existing classes.
2. Do not rename existing functions.
3. Keep file names unchanged.
4. Only modify code that is necessary to solve the problem.
5. Maintain compatibility with existing imports.
6. UI widgets and business logic should be separated when possible.
7. Avoid introducing unnecessary dependencies.
8. When editing Dart files, keep formatting consistent with Flutter conventions.
9. Prefer minimal diff changes rather than rewriting entire files.