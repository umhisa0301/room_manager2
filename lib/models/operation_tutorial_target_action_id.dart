/// 操作ガイドのハイライト対象タップ時に実行する画面遷移の種別。
enum OperationTutorialTargetActionId {
  /// 初期プロフィール設定（かんたん初期設定）。
  profileSetup,

  /// ROOMプロフィール編集（ニックネーム等）。
  profileEdit,

  /// ROOMタイプ診断。
  roomTypeDiagnosis,
}
